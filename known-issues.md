# 플러그인 테스트 이슈 (2026-07-03, 2026-07-08 업데이트)

## 1. 마이리얼트립 숙소 링크 403 에러 — 조치 완료
- 증상: 추천된 숙소 링크 클릭 시 403 에러 발생
- 조치: `recommend-mrt-products/SKILL.md`에 "링크를 보여주기 전 WebFetch로 접근 가능 여부를
  확인하고, 403/404/5xx면 클릭 가능한 링크 대신 '마이리얼트립에서 직접 검색해보라'는 안내로
  대체한다"는 절차를 추가했다. 상품 자체는 계속 보여주되 깨진 링크를 클릭 가능한 형태로
  내놓지 않는다.

## 2. 구글 연동 문제 — 원인 확인, 근본적으로 완전 자동화는 불가
- 증상: 구글 서비스(Drive/My Maps 등) 연동 시 직접 파일을 넣어야 하는 번거로움 있음
- 원인: Google My Maps는 지도를 프로그래밍적으로 생성하는 공개 API가 없다. OAuth로
  Drive에 파일을 올릴 수는 있어도, 그걸로 My Maps 지도가 자동 생성되진 않는다.
  (`sync-google-calendar`의 .ics 드래그&드롭도 같은 이유 — Google Calendar도 서드파티가
  이벤트를 자동으로 "받아들이게" 만드는 공개 임포트 API가 없다.)
- 조치: 이 제약을 스킬 문서에 명시하고, 마지막 임포트 단계(mymaps.google.com에 KML
  드래그&드롭)는 사용자가 직접 하도록 안내를 정직하게 바꿨다. 자동화 범위를 "파일 준비까지"로
  명확히 하는 것이 실제로 가능한 최선이다.

## 3. My Maps 저장 시 503 에러 — 조치 완료
- 증상: "My Maps에 저장해줘" 요청 시 503 에러 발생
- 실제 원인: `manage-travel-cart/SKILL.md`가 Drive 업로드 후 받은 **Drive 파일 ID**를 그대로
  `maps.google.com/maps/d/edit?mid=<file-id>` URL에 넣어 "지도 링크"라며 보여주고 있었다.
  Drive 파일 ID와 My Maps 지도 ID(`mid`)는 서로 다른 값이라 이 URL은 애초에 존재하지 않는
  지도를 가리켰고, 그래서 503/404가 났다 — 2번과 같은 "API 없음" 문제의 직접적인 결과였다.
- 조치: 존재하지 않는 `mid=` 링크를 만들어내는 로직을 제거했다. 이제 KML 파일만 로컬에
  준비하고, mymaps.google.com에서 직접 임포트하는 방법을 안내한다.
